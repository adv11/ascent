# Local Firebase project credentials — copy firebase.config.example.js instead
src/services/firebase.config.js

# OS cruft
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Local Claude Code settings (enabled plugins, etc.) — machine-specific, not a repo convention
.claude/settings.json
.claude/settings.local.json

# Dependencies (none currently, but this is a no-build project so keep it that way)
node_modules/

# Logs
*.log
npm-debug.log*

# Test artifacts
coverage/
test-results/
playwright-report/
